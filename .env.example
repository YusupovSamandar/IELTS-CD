# Database Provider Configuration
# Choose: 'local' | 'supabase' | 'production'
DATABASE_PROVIDER=local

# === LOCAL DOCKER POSTGRESQL ===
# Used when DATABASE_PROVIDER=local
LOCAL_DATABASE_URL="postgresql://ieltstrek_user:ieltstrek_local_password@localhost:5432/ieltstrek"
LOCAL_DIRECT_URL="postgresql://ieltstrek_user:ieltstrek_local_password@localhost:5432/ieltstrek"

# === SUPABASE CONFIGURATION ===
# Used when DATABASE_PROVIDER=supabase
# Setup Prisma checkout this guide: https://supabase.com/partners/integrations/prisma
SUPABASE_DATABASE_URL=XXXXXXXXX # Set this to the Transaction connection pooler string you copied in Step 1
SUPABASE_DIRECT_URL=XXXXXXXXX  # Set this to the Session connection pooler string you copied in Step 1

# === PRODUCTION DATABASE ===
# Used when DATABASE_PROVIDER=production
PRODUCTION_DATABASE_URL=XXXXXXXXX
PRODUCTION_DIRECT_URL=XXXXXXXXX

# === LEGACY SUPPORT (DEPRECATED) ===
# These are used by Prisma if the above are not set
# Will be removed in future versions
DATABASE_URL=${LOCAL_DATABASE_URL}
DIRECT_URL=${LOCAL_DIRECT_URL}

# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=XXXXXXXX

# Oauth
GITHUB_CLIENT_ID=XXXXXXXX
GITHUB_CLIENT_SECRET=XXXXXXXX

GOOGLE_CLIENT_ID=XXXXXXXX
GOOGLE_CLIENT_SECRET=XXXXXXXX

# Sending Email for verficiation using Resend. Checkout https://resend.com/api-keys
RESEND_API_KEY=XXXXXXXX

NEXT_PUBLIC_APP_URL="http://localhost:3000"